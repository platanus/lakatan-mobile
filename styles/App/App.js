import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  signoutButton: {
    position: 'absolute',
    bottom: 13,
    width: '100%',
    left: '25%',
  },
  contentContainer: {
    flex: 1,
  },
});

export default styles;
