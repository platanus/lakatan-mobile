import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  signoutButton: {
    position: 'absolute',
    bottom: '2%',
    width: '100%',
    marginLeft: '20%',
  },
  contentContainer: {
    flex: 1,
  },
});

export default styles;